class MyUser {
  final String uid;

  MyUser(this.uid);
}

class UserData {
  String uid;
  String firstName;
  String lastName;
  String nickname;

  UserData(this.uid, this.firstName, this.lastName, this.nickname);
}